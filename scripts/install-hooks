#!/bin/sh
# Wire scripts/bump-version as the repo's pre-commit hook.
set -e
root="$(git rev-parse --git-common-dir)"
mkdir -p "$root/hooks"
cp scripts/bump-version "$root/hooks/pre-commit"
chmod +x "$root/hooks/pre-commit"
echo "pre-commit hook installed → scripts/bump-version"
